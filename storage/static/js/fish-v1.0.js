function ENode(id, ueid, ecode, abstact, eclass, elevel, ebegin, eend) {
    var nd = {
        //id: id,
        ueid: ueid,
        ebegin: ebegin,
        eend: eend,
        //ecode: ecode,
        //eclass: eclass,
        //elevel: elevel,
        abstact: abstact
    };
    return nd;
}
